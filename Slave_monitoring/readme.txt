How can I monitor the slaves and trigger the mail if the slaves are offline in a scheduled time ?

The jenkins file provides the solution for it, here we monitor each nodes tied to jenkins agent and if the nodes are offline mail is triggered for the same.